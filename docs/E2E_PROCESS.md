# E2E_PROCESS.md — SUPERSEDED

Superseded by [/prd.md](../prd.md) on 2026-08-11. Do not use this file as process authority.

The end-to-end operating model — lifecycle, owner checkpoints, structured campaign objects, repo
boundaries, research model, revision/approval semantics, inter-repo handoffs, deliverables, and
system invariants — now lives in the canonical `prd.md`. Hard numeric rules live in
`SHOPYA_CAMPAIGN_CHARTER.yaml`; machine states/transitions live in
`schemas/workflow_state.schema.yaml`.

This tombstone exists only so existing links do not break. The prior content is preserved in git
history.
